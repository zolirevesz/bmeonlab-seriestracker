<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <data>
        <variable
            android:name="clickListener"
            android:type="android.view.View.OnClickListener"/>
        <variable
            android:name="series"
            android:type="android.revesz.seriestracker_v2.data.Series"/>
    </data>

    <android.revesz.seriestracker_v2.views.MaskedCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/card_side_margin"
        android:layout_marginEnd="@dimen/card_side_margin"
        android:layout_marginBottom="@dimen/card_bottom_margin"
        android:onClick="@{clickListener}"
        android:cardElevation="@dimen/card_elevation"
        android:cardPreventCornerOverlap="false"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Series.Card">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/plant_item_image"
                android:layout_width="0dp"
                android:layout_height="@dimen/show_item_image_height"
                android:contentDescription="@string/picture_of_show"
                android:scaleType="centerCrop"
                app:imageFromUrl="@{series.imageUrl}"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/plant_item_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_normal"
                android:layout_marginTop="@dimen/margin_normal"
                android:text="@{plant.name}"
                android:textAppearance="?attr/textAppearanceListItem"
                android:gravity="center_horizontal"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/plant_item_image"
                app:layout_constraintBottom_toBottomOf="parent"
                tools:text="Show"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </android.revesz.seriestracker_v2.views.MaskedCardView>

</LinearLayout>